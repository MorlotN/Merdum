
<% if @event.photo_url.present? && @event.photo_url != "festeam-logo.png"%>
  <% url = @event.photo_url %>
<% else %>
  <% url ="https://weraveyou.com/wp-content/uploads/2016/05/ushuaia_closing_party_ibiza_2016_featured.jpg" %>
<%end %>

<div class="banner-show" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= url %>);">
  <% if params["format"] %>

    <canvas id="canvas">
    </canvas>
  <% end %>
  <div class="container">
    <h1 id="nom-evenement"><%=@event.name%></h1>
    <h4 id="salle"><%=@event.address %></h4>
  </div>
</div>
<div class="show-event">
  <div class="container" id="event">
    <div class="col-md-8 col-lg-8">
      <div class="text">
        <h4>Description de l'événement :</h4>
<!--
        <p><%=@event.description%></p>
         -->
      </div>
    </div>
    <div class="col-md-3 col-lg-3">
      <div class="infos-global">
        <div class="infos">
          <p>Catégorie :</p>
          <h2><%= @event.category %></h2>
          <div class="date">
            <p>Date :</p>
            <% if @event.date_start != @event.date_end %>
              <p>Du <%= @event.date_start.strftime("%d %B %Y")%> <br> au <%= @event.date_end.strftime("%d %B %Y")%></p>
            <% else %>
            <p>Le</p>
              <p><%= @event.date_start %></p>
            <% end %>
          </div>
          <div class="infos-p">
            <h5>Plus d'informations sur<br>le site l'événement :</h5>
            <%= link_to "Site web", @event.event_url, target: :"_blank" %>

          </div>
        </div>
      </div>
    </div>
  </div>
</div>



